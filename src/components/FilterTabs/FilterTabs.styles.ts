// tslint:disable-next-line:import-name
import styled from 'styled-components';
import { colors } from '../../constants';

// tslint:disable-next-line
export const FilterTabsContainer: any = styled.div`
  background-color: ${colors.extended.blue800};
  margin: auto;
`;

// tslint:disable-next-line
export const FilterTabsHolder: any = styled.div`
  background-color: ${colors.extended.blue900};
  border-top-left-radius: 10px; 
  border-top-right-radius: 10px; 
`;
